export interface ProjectType {
    id?: string;         // ID проекта (опционально)
    icon?: string;       // Иконка проекта (опционально)
    header?: string;     // Заголовок проекта (опционально)
    description?: string; // Описание проекта (опционально)
}